'use strict';


describe('Set.superset(t)', () => {

  it('debería returnar true cuando todos los elementos de t están en el set');
  it('debería returnar false cuando no todos los elementos de t están en el set');

});
