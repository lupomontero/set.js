'use strict';


describe('Set.subset(t)', () => {

  it('debería returnar true cuando todos los elementos del set están en t');
  it('debería returnar false cuando no todos los elementos del set están en t');

});
